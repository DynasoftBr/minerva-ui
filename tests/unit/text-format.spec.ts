import { TextFormatter } from "@/text-format/text-format";
import { expect } from "chai";

describe("Text format", () => {
    it("String pattern", () => {
        const pattern = "AAA-0000";
        // const textFormat = new TextFormatter({ mask: pattern });
        // textFormat.RegExArray.forEach((el) => {
        //     console.log(el);
        // });

        expect(1).to.equals(1);
    });
});
